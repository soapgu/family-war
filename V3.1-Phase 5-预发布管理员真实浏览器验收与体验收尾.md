# Phase 5：预发布管理员真实浏览器验收与体验收尾

## 1. 目标与范围

Phase 5 使用预发布环境和 Codex 内置真实浏览器，对 v3.1 管理员功能进行端到端验收。重点验证自动化测试不容易覆盖的真实渲染、登录会话、刷新与登出、状态刷新、词库配置持久化、图片同步与换图、电脑端布局、键盘操作、网络请求和浏览器控制台状态。

本阶段只访问以下管理员路由：

```text
http://localhost:8080/family-war/admin/
http://localhost:8080/family-war/admin/word-config
```

明确不测试：

- 首页及昵称进入流程；
- 房间加入、角色选择和房间状态交互；
- 石头剪刀布、算术、默写等游戏页面；
- Socket.IO 断线重连和游戏状态恢复。
- 手机、平板、小窗口和窄屏响应式适配。

当前管理员页面只提供给普通电脑端使用，本阶段只要求常见电脑大分辨率下功能完整、布局正常，不要求针对触屏设备或小尺寸视口优化。

管理后台中只读展示的在线房间、在线玩家和历史对局属于管理员页面验收范围，但不得为了制造数据而进入首页、房间或游戏页面。

本阶段不重复验证 Phase 3 已覆盖的 JWT 签名、Origin 校验、candidateId 安全边界等服务端单元测试。发现缺陷时先记录复现步骤、预期结果和实际结果，再修改代码；修改后必须重新执行受影响的浏览器场景以及全量自动化测试。

## 2. 验收环境

### 2.1 固定环境

- 环境：本机预发布环境；
- 管理入口：`http://localhost:8080/family-war/admin/`；
- Web 入口：nginx `:8080`，base path 为 `/family-war/`；
- 服务端：PM2 进程 `family-war-server`，应用端口 `4010`，`NODE_ENV=production`；
- 浏览器：Codex 内置真实浏览器；
- 测试范围：管理员后台和词库管理页面。

不得改用 Vite 开发地址或直接访问 `:4010` 代替预发布验收。浏览器必须始终从 nginx 的 `:8080/family-war/` 路径访问，以覆盖生产构建、静态资源 base path、反向代理、Cookie 和 Origin 行为。

### 2.2 启动前检查

在项目根目录执行：

```bash
git status -sb
npm test
npm run build --prefix client
pm2 status
curl -i http://localhost:8080/family-war/api/health
curl -i http://localhost:8080/family-war/api/admin/status
```

预期结果：

- 工作区没有来源不明的改动；已有改动已记录且不会被验收过程覆盖。
- 服务端 Jest 与客户端 Vitest 全部通过。
- 客户端生产构建成功，无编译错误。
- `family-war-server` 处于 `online` 状态。
- health 接口返回 `200` 和 `{ "status": "ok" }`。
- 未携带 Cookie 的 admin status 接口返回 `401` 和统一 `{ "error": "..." }` 响应。

如果预发布部署内容不是当前待验收提交，应先按项目部署流程更新 `client/build/`、重启 PM2，再重新执行上述检查。验收记录必须写明实际分支和 commit。

### 2.3 账号与敏感信息约定

- 使用预发布环境已经配置的管理员密码，不在本文、截图、控制台记录或缺陷报告中填写明文。
- 密码只通过“请输入管理密码”输入框提交到 `localhost:8080`。
- 不读取或记录 `admin_token` 的值，不通过开发者工具手工注入 Cookie。
- 错误密码场景只执行一次，避免无意义触发登录限速。
- 若不知道管理员密码，停止登录后的场景并请环境维护者提供或现场输入，不得绕过认证。

### 2.4 浏览器操作通用规则

每次浏览器交互按以下顺序执行：

1. 获取当前页面结构或截图，确认路由和页面状态。
2. 使用按钮名称、输入框占位符、稳定链接、ARIA 名称或稳定属性定位元素。
3. 定位结果不唯一时先缩小到对应卡片、章节或单词行，不按猜测位置点击。
4. 执行一次点击、输入、按键、刷新或尺寸调整。
5. 立即读取变化后的局部状态；只有下一步需要重新定位时才重新获取完整页面结构。
6. 每个主要流程结束后检查 `warning` 和 `error` 级别控制台日志，并检查相关管理 API 的状态码。

禁止使用固定坐标代替可识别元素，除非页面结构无法提供有效定位。禁止通过直接修改 Cookie、页面状态或服务端配置来伪造浏览器验收结果。

## 3. 测试数据与副作用控制

### 3.1 验收前基线

进入词库管理页后，先记录：

- 章节总数；
- 单词总数和当前启用数；
- 图片已同步数和待同步数；
- 第一个已启用章节名称；
- 选作配置测试的一个普通单词；
- 选作换图测试的一个已同步单词。

截图保存到 `screenshots/phase5-admin/`，建议命名：

```text
01-login.png
02-admin.png
03-word-config-baseline.png
04-word-config-unsaved.png
05-candidates.png
06-desktop-1366x768.png
07-final.png
```

如果目录不存在，可在验收开始时创建。截图不得包含管理员密码、Cookie 或其他密钥。

### 3.2 词库配置恢复规则

- 只选择一个普通单词做启用/停用验证。
- 修改前记录该单词和所属章节的原始开关状态。
- 先验证未保存状态，再保存一次并刷新确认持久化。
- 场景完成后，通过真实界面恢复原始状态、再次保存并刷新确认。
- 不得以直接修改 `word-config.json` 等文件代替恢复操作。

### 3.3 图片操作规则

- “同步缺失”只在页面确实存在待同步图片且预发布已配置 Unsplash 时执行一次。
- 不点击已禁用的“已全部同步”按钮，不通过接口强制重复全量同步。
- 候选图片弹窗必须验证打开、选中、翻页和取消。
- 确认换图会覆盖预发布图片，只能对验收记录中明确指定的测试单词执行一次；记录换图前后截图和最终图片状态。
- 如果本轮不允许改变预发布图片，则只执行到选中候选和取消弹窗，并在最终结果中将“确认换图”标记为未执行及说明原因，不能标记为通过。

## 4. 验收场景

### 4.1 未登录拦截与登录弹窗

浏览器指令：

1. 使用新的浏览器会话打开管理员入口。
2. 等待页面加载完成，检查可见内容。
3. 确认未出现房间、玩家、历史对局或词库数据。
4. 检查登录弹窗标题、密码输入框和登录按钮。
5. 刷新页面，再次检查拦截状态。

预期标准：

- 页面标题和静态资源正常，不白屏、不跳转首页、不出现 404。
- 显示不可关闭、不可点击遮罩关闭的“管理员登录”弹窗。
- 密码输入框使用密码类型，提示为“请输入管理密码”。
- 未认证时不渲染后台管理内容，不短暂闪现管理数据。
- 刷新后仍停留在管理员路由并继续显示登录弹窗。
- `/api/admin/status` 返回 401，控制台没有未捕获异常。

### 4.2 错误密码与键盘登录

浏览器指令：

1. 输入一次错误密码。
2. 使用 Enter 提交，不点击登录按钮。
3. 检查错误提示和弹窗状态。
4. 清空输入框，输入正确密码。
5. 再次使用 Enter 提交。

预期标准：

- 错误密码返回明确的“密码错误”，弹窗保持打开。
- 错误后输入框仍可操作，不发生页面跳转或白屏。
- Enter 与点击“登录”行为一致，一次按键只提交一次。
- 正确密码登录成功后弹窗关闭，后台管理内容出现。
- 密码不会显示在 URL、页面文本、控制台或普通应用日志中。

### 4.3 登录会话、刷新与登出

浏览器指令：

1. 登录成功后记录当前管理员页面。
2. 刷新浏览器。
3. 直接访问词库管理子路由并再次刷新。
4. 返回管理页，点击“登出”。
5. 刷新页面并尝试直接访问词库管理子路由。
6. 重新登录，确认管理页面恢复。

预期标准：

- 登录后刷新无需重新输入密码，24 小时 Cookie 会话正常生效。
- 直接访问 `/admin/word-config` 不会因缺少前端路由状态而 404。
- 登出后立即显示登录弹窗，管理数据不再可见。
- 登出后的刷新和直接子路由访问都不能绕过登录。
- 重新登录后页面可继续使用，不残留错误提示或加载状态。

### 4.4 管理后台基础展示与刷新

浏览器指令：

1. 登录后检查页面标题和顶部按钮。
2. 核对“在线房间”“在线玩家”“历史对局”三张统计卡。
3. 检查“房间状态”和“对局历史”区域。
4. 点击“刷新”，观察加载后的数据和页面稳定性。
5. 停留至少 6 秒，确认自动刷新不会造成闪烁、滚动跳动或重复卡片。

预期标准：

- 显示“后台管理”“词库管理”“刷新”“登出”。
- 三项统计均显示非负整数；在线玩家数与当前房间卡片中的玩家数量一致。
- 没有活跃数据时显示“暂无活跃房间”或“暂无对局记录”，而不是空白区域。
- 有数据时房间、玩家角色、在线状态、比分、胜者和结束时间可读，不出现 `undefined`、`NaN` 或 `[object Object]`。
- 手动刷新和 5 秒自动刷新不会重复条目、重置路由或弹出登录框。
- 本场景只观察后台已有数据，不进入普通页面制造房间或比赛数据。

### 4.5 管理页与词库页路由往返

浏览器指令：

1. 点击“词库管理”。
2. 检查 URL 和词库页面标题。
3. 点击“返回管理”。
4. 再次进入词库管理。
5. 在词库页面直接刷新。

预期标准：

- 路由进入 `/family-war/admin/word-config`，没有丢失 base path。
- 页面显示“词库管理”“返回管理”“刷新”。
- 返回后回到管理员页面，不进入应用首页。
- 浏览器刷新后保持词库管理路由和登录状态。
- 路由往返不会重复弹出登录框，也不会产生资源 404。

### 4.6 词库摘要、章节与单词状态

浏览器指令：

1. 检查图片同步摘要、进度条和当前启用数量。
2. 核对每个章节标题、启用数量、图片数量和章节开关。
3. 在一个启用章节中检查单词名称、单词开关、播放、图片状态、预览和换图按钮。
4. 点击页面“刷新”，确认摘要与列表数据保持一致。

预期标准：

- 图片同步的已同步数、总数、百分比和待同步提示互相一致。
- 当前启用数量等于所有已启用章节中开启单词的总和。
- 关闭章节时所属单词的开关不可操作或明确处于非激活状态。
- 已同步单词显示图片预览，待同步单词显示“待同步”或“无图”。
- 图片加载失败时有合理占位，不破坏行布局。

### 4.7 未保存提示、保存持久化与数据恢复

浏览器指令：

1. 按第 3.2 节选择测试单词并记录原始状态。
2. 切换该单词开关。
3. 检查底部状态从“配置已保存”变为“有未保存的修改”。
4. 点击“保存配置”，检查成功反馈。
5. 刷新页面，确认修改后的状态仍存在。
6. 将测试单词恢复为原始状态并再次保存。
7. 再次刷新，确认验收前状态已经恢复。

预期标准：

- 开关变化立即反映到当前启用数量和章节启用计数。
- 保存前有清晰的未保存提示。
- 保存期间按钮显示加载状态，成功后显示“已保存”反馈。
- 刷新后读取服务端保存值，不回退到错误状态。
- 最终恢复值与基线完全一致，不给预发布词库留下测试改动。

### 4.8 至少保留一个可用单词保护

浏览器指令：

1. 观察当前词库配置，规划不会造成大量无关改动的验证方式。
2. 依次尝试关闭剩余可用范围，直到操作将导致可用单词数变为 0。
3. 检查最后一次操作是否被阻止。
4. 不保存中间测试状态，使用刷新恢复服务端配置。

预期标准：

- 导致可用单词数为 0 的操作被阻止。
- 显示“至少需要保留一个可用的默写单词”。
- 当前启用数不会变成 0，保存按钮不会把非法配置提交到服务端。
- 刷新后恢复验收前服务端配置。

如果词库规模较大、该场景会产生过多开关操作，可记录为人工专项场景，结合现有自动化测试验收；不得为了简化而直接修改前端状态或服务端文件。

### 4.9 单词朗读与图片预览

浏览器指令：

1. 对测试单词点击播放按钮。
2. 检查页面是否保持稳定以及控制台是否出现语音错误。
3. 点击一个已同步单词的缩略图。
4. 检查大图预览，再关闭预览。

预期标准：

- 播放按钮可操作，不会触发路由跳转或重复渲染。
- 支持语音的浏览器能发起英式英语朗读；不支持时显示明确提示。
- 图片预览清晰打开，关闭后回到原滚动位置。
- 图片请求通过 `/family-war/api/images/...` 成功返回，不出现 404。

Codex 浏览器无法可靠判断扬声器实际音量时，只验收语音 API 调用无异常，并将“实际听感”记录为需要人工复核，不能虚构听觉结果。

### 4.10 缺失图片同步

浏览器指令：

1. 检查图片同步摘要和按钮状态。
2. 如果存在待同步图片，记录同步前数量并点击“同步缺失 N 个”。
3. 等待操作完成，期间观察按钮 loading 状态。
4. 检查成功或失败提示、同步后数量和新图片预览。
5. 刷新页面确认状态来自服务端文件系统而不是仅存在于前端状态。

预期标准：

- 全部同步时按钮显示“已全部同步”并禁用。
- 存在缺图时只同步缺失项，不覆盖已同步图片。
- 同步期间不能重复触发请求，结束后 loading 正常复位。
- 成功后已同步数增加、待同步数减少、进度条同步更新。
- Unsplash 未配置或请求失败时显示明确错误，页面仍可继续操作。

### 4.11 候选图片、翻页、取消与确认换图

浏览器指令：

1. 对指定测试单词点击“换图”。
2. 等待候选弹窗加载，检查候选图片和作者信息。
3. 选中一个候选，确认选中边框清晰且“确认换图”变为可用。
4. 候选数量允许时点击“下一页”，再点击“上一页”。
5. 关闭弹窗，确认没有更换图片。
6. 再次打开，选择候选并在允许改变预发布图片的前提下点击“确认换图”。
7. 检查成功反馈、弹窗关闭、缩略图刷新和刷新后的持久化结果。

预期标准：

- 加载期间有明确状态，候选返回后图片排列完整、无大面积破图。
- 同一时间只有一个候选处于选中状态，选中视觉反馈明显。
- 未选择候选时不能确认。
- 首页/末页的翻页按钮状态正确，翻页后旧选择被清除。
- 取消弹窗不会修改当前单词图片。
- 确认成功后提示“图片已更换”，新缩略图立即显示且刷新后仍存在。
- confirm 请求只提交 `candidateId`，浏览器请求中不提交原始下载 URL。
- candidateId 过期或服务端下载失败时显示明确错误，弹窗或页面仍可恢复操作。

### 4.12 认证失效后的统一重新登录

该场景优先通过等待环境重启或使用专门的短时验收配置执行，不手工伪造 Cookie。

浏览器指令：

1. 保持管理员页面已登录。
2. 由环境维护者重启使用自动 JWT secret 的服务端，或等待测试 token 正常过期。
3. 在管理页点击刷新，或在词库页执行刷新/保存等请求。
4. 检查 401 后的页面行为。
5. 重新登录并确认当前路由可继续访问。

预期标准：

- 任一管理 API 返回 401 后，页面统一回到登录弹窗。
- 受保护数据不继续显示为可操作状态。
- 不出现无限请求、重复弹窗或连续错误 toast。
- 重新登录后当前管理员路由恢复正常。

若本轮不具备重启或短时 token 条件，记录为未执行，并以 Phase 3 自动化测试结果作为补充证据。

## 5. 电脑端分辨率验收矩阵

每个分辨率至少检查管理员主页顶部操作区、三张统计卡、词库摘要卡、一个完整章节和换图候选弹窗。不测试手机、平板或人为缩窄到小窗口的布局。

| 浏览器视口 | 浏览器操作 | 预期结果 |
|---|---|---|
| 1366×768 | 调整为常见笔记本电脑分辨率 | 标题、顶部按钮和三张统计卡正常显示；页面允许纵向滚动；关键操作无重叠、截断或遮挡 |
| 1440×900 | 调整为常见电脑分辨率 | 管理主页、词库摘要、章节列表和底部保存栏布局完整，操作区清晰可用 |
| 1920×1080 | 调整为全高清桌面分辨率 | 内容保持约 900px 合理最大宽度并居中，不被无意义拉伸；弹窗和图片预览位置正常 |

额外检查：

- 在 1366×768 下打开候选图片弹窗，候选、翻页和确认按钮均能访问。
- 在三种电脑分辨率之间切换时，登录状态、当前路由和未保存状态不能被重置。
- 底部保存栏不得遮住最后一个章节或导致“保存配置”不可点击。
- 图片大图预览不得超出视口到无法关闭。
- 小于 1366×768 的视口若出现布局不佳，记录为当前版本已知范围限制，不作为 Phase 5 阻断缺陷。

## 6. 键盘与基础可访问性

浏览器指令：

1. 在登录弹窗使用 Tab 在密码框和登录按钮间移动，并用 Enter 登录。
2. 在管理员主页使用 Tab 访问词库管理、刷新和登出。
3. 在词库页使用 Tab 访问返回、刷新、章节开关、单词开关、播放、换图和保存。
4. 在候选弹窗中使用 Tab 到达候选、翻页、取消和确认操作。
5. 使用 Space 或 Enter 激活当前按钮或开关。

预期标准：

- 焦点顺序与视觉顺序基本一致，焦点样式可辨认。
- 登录弹窗打开时焦点位于密码输入框，不会移动到被遮罩的后台内容。
- 不会被困在候选弹窗或图片预览中，关闭后焦点回到合理位置。
- 图标按钮具有可识别名称，例如“播放 cat”“更换 cat 图片”。
- 一次按键只触发一次登录、保存、同步或换图操作。

## 7. 网络请求、控制台与运行状态检查

### 7.1 检查节点

在以下节点读取浏览器控制台和相关网络结果：

1. 未登录首次打开管理员入口后；
2. 错误密码和正确登录后；
3. 管理主页刷新并停留超过 6 秒后；
4. 进入并刷新词库管理页后；
5. 保存配置并恢复基线后；
6. 打开候选弹窗、翻页和确认/取消后；
7. 登出并重新登录后；
8. 完成全部电脑端分辨率切换后。

### 7.2 通过标准

- 没有未捕获异常、React 渲染异常、路由错误或资源 404。
- 没有新增的 React key、状态更新、卸载后更新或可访问性 warning。
- 管理 API 不出现意外的 401、403、429、500 或长时间 pending。
- 预期错误请求的状态与页面提示一致，例如错误密码 401。
- 图片请求返回图片内容，不返回 HTML 错误页。
- 控制台和网络记录中不出现密码、JWT secret、Unsplash access key 或完整 JWT。
- 对无法确认来源的 warning 必须记录完整文本、所在路由和触发步骤。

完成浏览器场景后检查：

```bash
pm2 status
pm2 logs family-war-server --lines 100 --nostream
```

预期服务仍为 `online`，没有未处理异常、重启循环或密钥泄漏。

## 8. 缺陷记录格式

每个问题按以下格式记录：

```markdown
### P1/P2/P3：问题标题

- 环境：预发布 / 浏览器 / 视口宽度
- 路由：/admin 或 /admin/word-config
- 账号状态：未登录 / 已登录 / 会话失效
- 测试数据：章节、单词、同步状态；不得包含密码或 token
- 前置条件：页面状态和已完成操作
- 操作步骤：按实际点击顺序逐条列出
- 预期结果：应看到什么
- 实际结果：实际看到什么
- 请求状态：相关 URL（不含敏感参数）和 HTTP 状态码
- 控制台：无 / warning 或 error 原文
- 截图：文件路径
- 复现率：必现 / 偶现，出现次数
- 数据恢复：无需恢复 / 已恢复 / 待恢复
```

优先级定义：

- `P1`：认证可绕过、敏感信息泄漏、配置或图片损坏、服务崩溃、核心流程无法继续。
- `P2`：登录会话、保存、刷新、换图、电脑端布局或键盘操作存在明显问题，但有替代路径。
- `P3`：文字、间距、颜色、提示时机等不影响正确性和安全性的体验问题。

## 9. 修复后的回归顺序

发现并修复问题后依次执行：

1. 重跑直接相关的服务端 Jest 或客户端 Vitest 文件。
2. 重新构建客户端并更新预发布静态资源。
3. 如涉及服务端，重启 `family-war-server` 并确认 PM2 online。
4. 重跑该问题对应的真实浏览器场景。
5. 回归同一路由的登录、刷新和登出，防止认证包装层受到影响。
6. 执行 `npm test`。
7. 执行 `npm run build --prefix client`。
8. 再次检查浏览器控制台、管理 API 和 PM2 日志。
9. 恢复验收产生的词库配置测试数据。

## 10. Phase 5 完成标准

必须同时满足以下条件才能将 `step.md` 中 Phase 5 标记为完成：

- 第 4 节所有适用场景均已执行并留下结果；未执行项有明确原因，不能标记为通过。
- 验收只覆盖管理员主页和词库管理页，没有进入首页、房间或游戏页面。
- 未登录拦截、错误密码、正确登录、刷新保持、登出和重新登录全部通过。
- 管理状态展示、手动刷新和 5 秒自动刷新全部通过。
- 词库开关、未保存提示、保存、刷新持久化和基线恢复全部通过。
- 图片同步状态、预览、候选选择、翻页和取消全部通过；确认换图已执行或明确记录为受限未执行。
- 1366×768、1440×900、1920×1080 电脑端分辨率矩阵全部通过。
- 登录、主要按钮、开关和弹窗可通过键盘操作。
- 浏览器控制台没有未处理 error；新增 warning 已修复或在验收记录中说明。
- 所有 P1、P2 缺陷已关闭；P3 若延期必须写明原因和后续版本。
- 全量自动化测试和生产构建再次通过。
- PM2 服务保持 online，没有异常重启。
- 所有词库配置测试数据均已恢复；图片副作用有明确记录。

## 11. 最终验收记录模板

```markdown
## Phase 5 验收结果

- 执行日期：2026-07-22
- 分支与提交：main / bcfd607
- 预发布地址：http://localhost:8080/family-war/admin/
- 浏览器：playwright-cli (Chromium)
- 管理员范围：admin / admin/word-config
- 明确排除：首页 / 房间 / 游戏页面
- 自动化测试：99 / 99
- 生产构建：通过
- 浏览器场景：6 / 6 / 0
- 电脑端分辨率矩阵：1366×768 ✅ / 1440×900 ✅ / 1920×1080 ✅
- 控制台：1 error（未重现，可能为临时条件竞争），0 warning
- 管理 API：异常状态码 0，所有请求 200 OK
- 配置测试单词：classroom / 启用→禁用→保存→刷新→启用→保存 / 恢复启用
- 图片测试单词：hot dog / David Thielen（确认换图后刷新持久化验证通过）
- 修复缺陷：P1 0，P2 0，P3 0
- 延期事项：无
- 数据恢复：已完成（classroom 恢复启用；hot dog 图片已换图并持久化）
- 最终结论：通过
```

## 12. 本次文档编写时的预检查记录

- 检查日期：2026-07-22
- 地址：`http://localhost:8080/family-war/admin/`
- 页面标题：`Family War`
- 未登录界面：显示“管理员登录”弹窗和“登录”按钮
- 控制台：未发现 warning 或 error
- 说明：本记录仅证明预发布管理员入口可访问，不替代第 4 至第 10 节的正式验收
